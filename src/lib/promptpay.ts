/**
 * PromptPay QR — ย้ายไปอยู่ที่ @platform/core แล้ว
 *
 * โค้ดจริงอยู่ที่ micro-services/packages/core/src/promptpay.ts
 * สำเนาที่ใช้ build อยู่ที่ src/lib/_core/promptpay.ts (ไฟล์ generate — ห้ามแก้)
 *
 * ไฟล์นี้เหลือไว้เป็นทางเข้าเดิม ทุกที่ที่ import "@/lib/promptpay" จึงไม่ต้องแก้
 */
export { buildPromptPayPayload } from "./_core/promptpay";
